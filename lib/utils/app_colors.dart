import 'package:flutter/material.dart';

// Central place for the app's brand colors, so every screen uses the same
// shade instead of copy-pasting hex codes everywhere. If we ever want to
// re-brand the app, we only change it here.
class AppColors {
  // The indigo/blue-violet used for active nav items, buttons, and links
  // throughout the app (matches the sample UI design we were given).
  static const Color primary = Color(0xFF4F46E5);

  // Light grey background used behind search bars and unselected chips.
  static const Color lightGrey = Color(0xFFF1F1F5);
}
